import { Injectable } from '@angular/core';
import { StateCollection } from '../../helpers/state.collection';
import { Produtos } from '../../produtos/models/produtos.model';

@Injectable({ providedIn: 'root' })
export class VendasState {

}